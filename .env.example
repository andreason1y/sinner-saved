# Supabase (Phase 3 will activate these)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Google Analytics 4 measurement ID (format: G-XXXXXXXXXX).
# Optional — leave empty to disable GA. Only loads on public pages.
NEXT_PUBLIC_GA_ID=

# Groq API key for AI features (tag generator & sub-category suggestion).
GROQ_API_KEY=
